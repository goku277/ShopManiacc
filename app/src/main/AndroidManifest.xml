<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.biswadeep.developer.shopmaniacc">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/shopmaniacc_logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/shopmaniacc_logo"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Electronics"></activity>
        <activity
            android:name=".Cart"
            android:label="cart" />
        <activity android:name=".Payment_Activity" />
        <activity
            android:name=".Food"
            android:label="Order foods" />
        <activity
            android:name=".Books"
            android:label="Oredr books" />
        <activity
            android:name=".Cloth"
            android:label="Buy clothes" />
        <activity android:name=".MainActivity" />
        <activity
            android:name=".Register"
            android:label="Signup" />
        <activity
            android:name=".Login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>